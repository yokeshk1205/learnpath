[project]
name = "learnpath-ml"
version = "0.16.0"
description = "LearnPath model training and inference service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
